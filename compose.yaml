# networks:
#   app_net:
#     driver: bridge
#     external: false

# volumes:
#   pgdata:
#     driver: local

services:
  linux-builder:
    build:
      context: .
    image: builder:linux
    container_name: build_linux
    volumes:
      - ./apps/ziyun/release:/output_dist
