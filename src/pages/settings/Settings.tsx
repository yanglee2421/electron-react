import { Card, CardContent, CardHeader, Grid2 } from "@mui/material";

export const Settings = () => {
  return (
    <Card>
      <CardHeader title="Settings" />
      <CardContent>
        <Grid2 container spacing={6}></Grid2>
      </CardContent>
    </Card>
  );
};
// Joney
